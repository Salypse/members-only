<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Message Board</title>
    <link rel="stylesheet" href="/styles/index.css" />
  </head>
  <body>
    <%- include("partials/nav") %>
    <% if (locals.user) { %>
      <% if (user.member) { %>
        <p>Welcome, Club Member!</p>
      <% } %>
    <% } %>
  </body>
</html>
