<% if (locals.errors?.length || locals.messages?.length) { %>  
    <ul>
        <% if (errors?.length) { %>
            <% errors.forEach((error) => { %>
                <li><%= error.msg %></li>
            <% }) %>
        <% } %>

        <% if (messages?.length) { %>
            <% messages.forEach((message) => { %>
                <li><%= message %></li>
            <% }) %>
        <% } %>
    </ul>
<% } %>